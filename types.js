// Типы данных (простые типы):
// Числа

let number = 13;
console.log(-4/0);
console.log('string' * 9);

// Строка

const persone = 'Andrew';

// Логический тип (boolean)

const bool = true;

// null и undefined

console.log(something);

let und;
console.log(und);

// Типы данных (комплексные/объекты):

const obj = {
    name: 'John',
    age: 25,
    isMarried: false
};

console.log(obj.name);



// Массив (частный случай комплексного объекта)

let arr = ['plum.png', 'orange.jpg', 6, 'apple.bmp', {}, []];
console.log(arr[1]);